export const mockAuthService = {
  validateUser: jest.fn(),
  checkPassword: jest.fn(),
  login: jest.fn(),
  logout: jest.fn(),
  refresh: jest.fn(),
  socialLogin: jest.fn(),
  validateAdmin: jest.fn(),
};
